extends layouts/main.pug

block content
    #signin
        h3.ins Connexion
        .formIn
        form(action='post' ,='')
            .inpu
            input(type='text' ,class='inpu' ,name='email' placeholder='Courriel')
            input(type='password' ,class='inpu' ,='' name='password' placeholder='Mot de passe')
            if error
                p(style='color:red;') error
            button.but Connexion
        p#deja
            | Pas de compte ? 
            a(href='') Inscrivez_vous